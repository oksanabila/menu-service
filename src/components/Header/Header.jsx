import React from 'react';
import css from './Header.module.scss';

const Header = () => {
    return (
        <header>
            <div style={css.container}>

            </div>
        </header>
    );
};

export {Header};